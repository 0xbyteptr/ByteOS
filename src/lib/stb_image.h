/* Included upstream stb_image.h (header only) */
/* We'll include the real implementation in one .c file using STB_IMAGE_IMPLEMENTATION */
/* For brevity, the full header is not copied here; the Makefile will download the official stb_image.h when building if not present. */
#ifndef STB_IMAGE_H_LOCAL
#define STB_IMAGE_H_LOCAL
#include "stb_image_stub.h"
#endif
