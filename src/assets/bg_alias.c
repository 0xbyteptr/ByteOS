#include "bg.h"

/* If the generated assets_bg_* symbols are not present (e.g. no assets/bg.bmp
   was converted) then this file is a no-op.  The real aliases are defined in
   src/assets/bg.c when that file is present. */

/* Nothing to do here intentionally. */
